import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  googleButtonContainer: {
    alignContent: 'center',
    marginVertical: 10,
  },
  googleButton: {
    borderRadius: 20,
    padding: 5,
    width: 280,
  },
});

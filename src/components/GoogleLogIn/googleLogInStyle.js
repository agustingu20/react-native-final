import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  googleButtonContainer: {
    alignContent: 'center',
    marginVertical: 10,
    marginHorizontal: 70,
  },
  googleButton: {
    borderRadius: 6,
  },
});
